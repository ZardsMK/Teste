let text = "leetCode";

const inverter = (texto) => {
    let array = [], i, textoF = '';
    for(i = texto.length - 1; i >= 0; i--){
        array.push(texto[i]);
    }
    array = array.join('')
    console.log(array)
}

inverter(text)
